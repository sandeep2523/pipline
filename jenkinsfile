 pipeline {
    agent any
    parameters {
        choice(choices: ['BUILD ', 'DEPLOY', 'TEST'], description: 'Please click on particular stage to run', name: 'SELECT THE STAGE')
    }
    stages {
        stage('BUILD') {
            steps {
             echo "this is build stage"
            }
        }
    }
}
