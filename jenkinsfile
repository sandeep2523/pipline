 pipeline {
    agent any
    parameters {
        string(defaultValue: 'BUILD', description: 'Click on this to run this stage', name: 'Stage Name')
    }
    stages {
        stage('BUILD') {
            steps {
             echo "this is build stage"
            }
        }
    }
}
